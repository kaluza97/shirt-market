import CartWrapper from '@/components/Cart/Cart.component';
import React, { FC } from 'react';

const Cart: FC = () =>
(
    <>
        <h2>Cart</h2>
        <CartWrapper />
    </>
);

export default Cart;
